<%= form_for(@phone_session_type) do |f| %>
  <% if @phone_session_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@phone_session_type.errors.count, "error") %> prohibited this phone_session_type from being saved:</h2>

      <ul>
      <% @phone_session_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
